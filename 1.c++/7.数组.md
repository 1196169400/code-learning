**理解复杂的数组声明：**

原则：从里到外，从右到左；

int *(&arry)[10] = ptrs;

按照从里到外的顺序阅读上述语句，首先知道array是一个引用，然后观察右边知道，array引用的对象是一个大小为10的数组，最后观察左边知道，数组的元素类型是指向int的指针。这样，array就是一个含有10个int型指针的数组的引用。

注：没有引用数组，例如  int &a[10]={b,c};