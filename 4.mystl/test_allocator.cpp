#include "allocator.h"

#include<iostream>
#include<string>
//using namespace std;

/*
int main()
{
    mystl::allocator<int> alloc;
    size_t a{2};
    auto const p = alloc.allocate(a); 
    /*
    auto q = p;
    alloc.construct(q);
    alloc.construct(q++,1);
    do {
        alloc.destroy(q);
    }while(q-- != p);
    alloc.deallocate(p, 2);
    
    return 0;
}
*/
